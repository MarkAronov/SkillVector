import {
	Button as ShadcnButton,
	buttonVariants as shadcnButtonVariants,
} from "@/components/ui/button";

/**
 * Button Component
 *
 * Re-exports shadcn/ui Button with SkillVector's existing customizations.
 * All customizations are maintained in the variants below.
 */

export { ShadcnButton as Button, shadcnButtonVariants as buttonVariants };
